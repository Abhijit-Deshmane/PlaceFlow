/**
 * @placeflow/mobile — constants barrel export
 *
 * Import design tokens from here in feature code:
 *   import { theme, colors, semantic, spacing } from "@/constants";
 */
export * from "./theme";
